# Prueba Sinergia - Angular + Laravel  + MySQL

Crud made with Angular + Laravel.

## Installation 
After download install composer

```bash
npm install
composer install
php artisan migrate
php artisan migrate:refresh --seed
```

Developer By Juan Sebastian Murcia Zuñiga
